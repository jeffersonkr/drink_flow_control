version: '3'
services: 
    nginx: 
        image: nginx:latest
        container_name: nginx
        ports:
         - 80:80
        volumes:
          - ./config/nginx/conf.d:/etc/nginx/conf.d
        restart: always